TODO
  Add arg support "build.sh -d OnePlus3 -v 1.3 ..." verbose / push / flash
  Add more divine mod
  f2fs?
  script adb pull adaway & more..
  Gapps update
  Custom kernel
  No sleep ?
  system/etc GPS NTP server
  copy cert on server
  apktool
  adb push zip to device yes or no option

openssl genrsa -out key.pem 1024
openssl req -new -key key.pem -out request.pem
openssl x509 -req -days 9999 -in request.pem -signkey key.pem -out certificate.pem
openssl pkcs8 -topk8 -outform DER -in key.pem -inform PEM -out key.pk8 -nocrypt
